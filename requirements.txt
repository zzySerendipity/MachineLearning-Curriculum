matplotlib==3.11.0
numpy==2.4.6
pandas==3.0.3
Pillow==12.2.0
# If not using PyQt6 frontend in Experiment2, you may skip this package (it is very large and not recommended)
# PyQt6==6.11.0
scikit_learn==1.9.0
seaborn==0.13.2
statsmodels==0.14.6
# PyTorch version does not need to be too recent; older stable versions work fine
torch==2.8.0
torchvision==0.23.0